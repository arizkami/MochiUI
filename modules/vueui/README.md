# vueui

To install dependencies:

```bash
bun install
```

To build the reusable module bundle:

```bash
bun run bundle
```

To build the native demo bundle from `example/App.vue`:

```bash
bun run bundle:demo
```
